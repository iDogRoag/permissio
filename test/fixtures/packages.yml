name: Packages

on:
  push:

jobs:
  ghcr:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/build-push-action@v6
        with:
          push: true
          tags: ghcr.io/example/permissio:latest
