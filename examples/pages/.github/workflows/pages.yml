name: Pages

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "build site"

  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/deploy-pages@v4
